<h2> Welcome <%= @user.username %> !</p>

<p> Here are the resources you've added: </p>
<ol>
  <% @user.resources.each do |resource|%>
    <li><%= link_to resource.title, resource_path(resource.id) %></li>
  <% end %>
</ol>

<%= link_to "Add a new resource", new_resource_path %>
