<% if @resource.comments.any? %>
  <% i = 1 %>
  <% @resource.comments.each do |f| %>
    <% if f.user %>
      <article class="comment"><%= f.text %><br>
      <small><%= f.user.username %></small></article>
    <% end %>
  <% end %>
<% end %>