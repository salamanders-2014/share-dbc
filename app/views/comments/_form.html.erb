<%= form_for([@resource, @resource.comments.build]) do |f| %>
  <p>
    <%= f.label :text, "Comment on this resource:" %><br>
    <%= f.text_area :text %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
