
<div class="show_resource">
  <h1><%= link_to(@resource.title, "http://#{@resource.link}") %></h1>
  <span class="creator"><%= @creator.first_name %> <%= @creator.last_name %></span><%= button_to "Upvote", resource_upvote_path(@)
  <% if @creator.id == @user.id %>
  <small><%= link_to 'edit', edit_resource_path(@resource) %> |  <%= link_to 'delete' , delete_resource_path(@resource) %></small>
  <% end %>
  <div class="resource_description">
    <p>
      <%= @resource.description %>
    </p>
  </div>
</div>

